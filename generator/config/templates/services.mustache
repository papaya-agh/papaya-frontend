/* This file is automatically generated */

const express = require('express');
const impl = require('./impl.js');
const app = express();

const basePath = '/api';

{{#services}}
{{#serviceOperations}}const {{operationName}}Path = basePath + '{{{operationPath}}}';{{/serviceOperations}}

app.use(express.json());
{{#serviceOperations}}
app['{{operationMethod}}'.toLocaleLowerCase()]({{operationName}}Path.replace(/{/g, ':').replace(/}/, ''), (req, res) => {
    return res.send(impl.{{operationName}}(req.body, req.query));
});

{{/serviceOperations}}
{{/services}}
app.listen(8080, () =>
    console.log(`Listening on port 8080`),
);
